<UserControl x:Class="SalesApp.Wpf.Views.OrdersKanbanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Command="{Binding ShowGridCommand}" Content="📊 شبكة"/>
        </StackPanel>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Items}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="220" Margin="6" Padding="12"
                            CornerRadius="6"
                            Background="{DynamicResource App.SurfaceBrush}"
                            BorderBrush="{DynamicResource App.BorderBrush}"
                            BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="{Binding Key}" FontWeight="Bold"
                                       Foreground="{DynamicResource App.PrimaryBrush}"/>
                            <TextBlock Text="{Binding CustomerName}" Margin="0,4,0,0"/>
                            <TextBlock Text="{Binding Amount, StringFormat={}{0:N2} ر.س}" 
                                       FontWeight="SemiBold" Margin="0,4,0,0"/>
                            <Border Margin="0,6,0,0" Padding="6,2" CornerRadius="3"
                                    Background="{DynamicResource App.PrimaryBrush}"
                                    HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Status}"
                                           Foreground="{DynamicResource App.TextInverseBrush}"
                                           FontSize="11"/>
                            </Border>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>